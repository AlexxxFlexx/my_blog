{% extends 'base.html.twig' %}

{% block title %}Nouvelle catégorie{% endblock %}

{% block body %}
    <body class="bg-dark bg-gradient">
        <div class="container bg-light rounded p-5 mt-4">
            <h1 class="fs-2 text-center pb-2">🏴‍☠️ Créer une nouvelle catégorie 🏴‍☠️</h1>
            <hr class="pb-3">

            <div class="row justify-content-center">
                <div class="col-md-8">
                    {{ form_start(form) }}
                        <div class="mb-3">
                            {{ form_label(form.name, 'Nom de la catégorie', {'label_attr': {'class': 'form-label'}}) }}
                            {{ form_widget(form.name, {'attr': {'class': 'form-control shadow-lg mb-3', 'placeholder': 'Entrez le nom de la catégorie'}}) }}
                            
                            {{ form_label(form.description, 'Description', {'label_attr': {'class': 'form-label'}}) }}
                            {{ form_widget(form.description, {'attr': {'class': 'form-control shadow-lg', 'placeholder': 'Entrez la description de la catégorie'}}) }}
                        </div>
                        <button class="btn btn-success rounded-pill shadow-lg w-100">Créer la catégorie</button>
                    {{ form_end(form) }}

                    <a href="{{ path('categories') }}" class="btn btn-outline-danger rounded-pill shadow-lg w-100 mt-3">Retour à la liste</a>
                </div>
            </div>
        </div>
    </body>
{% endblock %}