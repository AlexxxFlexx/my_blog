{% extends 'base.html.twig' %}

{% block title %}Mon Profil
{% endblock %}

{% block body %}
	<head>
		<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
	</head>
	<body class="bg-dark bg-gradiant">
		<div class="bg-light pb-4 mb-5 rounded">
			<h1 class="text-center">🥷 - Mon Profil - 🥷</h1>
			<div class="container mt-2 pt-2">
				<div class="card mb-3">
					<div class="row g-0">
						<div class="col-md-4">
							<img src="{{ app.user.profilePicture }}" class="img-fluid rounded-start" alt="Photo de profil">
						</div>
						<div class="col-md-8">
							<div class="card-body">
								<h5 class="card-title">{{ app.user.firstName }}
									{{ app.user.lastName }}</h5>
								<p class="card-text">
									<strong>Email:</strong>
									{{ app.user.email }}</p>
								<p class="card-text">
									<strong>Date de création:</strong>
									{{ app.user.createdAt|date('d/m/Y') }}</p>
								<div class="mt-3">
									<a href="{{ path('app_edit_profile') }}" class="btn btn-dark rounded-pill shadow-lg">
										<i class="fas fa-edit"></i>
										Modifier le profil
									</a>
								</div>
							</div>
						</div>
					</div>
				</div>
			</div>
		</div>
	</body>
{% endblock %}
